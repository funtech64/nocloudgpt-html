# NoCloudGPT Model Family Guide (Strict Edition)

**Purpose**  
This is an internal editorial reference for humans only.  
It defines consistent voice, structure, and buyer-friendly language for `/models/` pages.

**Critical Rule for AI Agents (Cursor / Codex / Claude)**  
When a human gives a **precise single-file edit instruction** (e.g. “insert exactly this section after this tag and return only the updated file”), you must follow it **exactly**.  
Do **not**:
- Scan flat lists or this guide to decide if work is already done
- Report global status across pages
- Make assumptions about other files
- Perform “cleanup passes” or status updates unless explicitly asked
- Output full files when only a minimal insertion was requested

Follow the user’s exact instructions first. This guide is secondary.

---

## Core Editorial Rules (Still Apply)

Each model family page should clearly answer:
1. What is this model family good at?
2. Who should choose it?
3. What kind of private AI deployment does it fit?
4. What are the licensing or availability cautions?
5. What NoCloudGPT action should the visitor take next?

### Page Status Labels (Human Use Only)
Use these internally in this guide or as HTML comments only.  
Never show them in rendered public copy.

- Publish-ready
- Needs polish
- Needs verification
- Draft / noindex
- Legacy / duplicate

### Strict Editorial Rules
- Never leave visible `VERIFY`, `TODO`, or draft language in rendered pages.
- Convert useful internal notes to HTML comments only.
- Do not invent benchmarks, performance claims, license terms, or hardware requirements.
- Use buyer-friendly language and NoCloudGPT deployment tier language.
- Every public page must have a clear CTA.

### Recommended CTAs
- Build your deployment quote
- Request a NoCloudGPT quote
- Start a private AI pilot
- Plan an on-premise installation
- Compare model families
- Choose the right model for your workload

---

## Current Focus (June 2026)

Primary goal right now: Clean, consistent model family pages with precise section insertions only.

**When performing a precise edit requested by the user, ignore all status labels and flat lists. Perform only the requested change.**
